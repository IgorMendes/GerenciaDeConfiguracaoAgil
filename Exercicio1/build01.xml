<project name="AtvGerenciaConfigAgil" default="main" basedir=".">

<description>Projeto para aula de Gerencia de Confi Agil</description>

<!-- global properties for this build file -->
<property name="source.dir" location="src"/>
<property name="build.dir" location="bin"/>
<property name="doc.dir" location="doc"/>
<property name="lib.dir" location="lib"/>

<junit printsummary="yes" haltonfailure="yes">
	<classpath id="junit.class.path">
		<fileset dir="${lib.dir}">
			<include name="**/junit-4.13.jar" />
		</fileset>
	</classpath>
</junit>
	
<!-- set up some directories used by this project -->
	<target name="init" description="setup project directories">
		<mkdir dir="${build.dir}"/>
		<mkdir dir="${doc.dir}"/>
</target>

<!-- Compile the java code in ${src.dir} into ${build.dir} -->
<target name="compile" description="compile java sources">
	<javac srcdir="${source.dir}" destdir="${build.dir}" includeantruntime="false">
</javac>
</target>

<!-- Generate javadocs for current project into ${doc.dir} -->
<target name="doc" description="generate documentation">
	<javadoc sourcepath="${source.dir}" destdir="${doc.dir}/docs">
		<classpath refid="junit.class.path" />
</javadoc>
</target>
	<!-- fim -->
</project>